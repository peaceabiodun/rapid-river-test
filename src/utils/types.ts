export type ArticleFormDataType = {
  name: string;
  email: string;
  body: string;
};

export type ArticleType = {
  postId: number;
  id: number;
  name: string;
  email: string;
  body: string;
};
